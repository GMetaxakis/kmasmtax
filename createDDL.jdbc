CREATE TABLE ADMIN.BILLS (NUMBER INTEGER NOT NULL, AFM VARCHAR(9) NOT NULL, AMOUNT FLOAT NOT NULL, COMNAME VARCHAR(30) NOT NULL, DATE VARCHAR(20) NOT NULL, USERID INTEGER NOT NULL, CATNAME VARCHAR(30), PRIMARY KEY (NUMBER))
